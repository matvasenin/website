---
import type { HTMLAttributes } from "astro/types"

interface Props extends HTMLAttributes<"article"> {
  color?: "paper" | "primary" | "accent";
}
const { color, ...rest } = Astro.props;
---

<article class:list={["card", color, rest.class]} {...rest}>
  <slot />
</article>

<style is:global>
  .card {
    border: var(--border-width) solid var(--color-primary);
    padding: 1rem;
    &.paper {
      background: var(--color-paper);
      color: var(--color-primary);
    }
    &.primary {
      background: var(--color-primary);
      color: var(--color-paper);
      &::selection {
        background: var(--color-paper);
        color: var(--color-primary);
      }
    }
    &.accent {
      background: var(--color-accent);
      color: var(--color-paper);
    }
  }
  .card-stack-x > *:not(:last-child) {
    margin-inline: 0 calc(-1 * var(--border-width));
  }
  .card-stack-y > *:not(:last-child) {
    margin-block: 0 calc(-1 * var(--border-width));
  }
</style>